---
description: Record a gotcha/landmine you just hit so future you avoids it.
---

Call the `get-command-instructions` MCP tool with `command: "note-gotcha.md"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/note-gotcha.md` and follow those instructions.

If the cloud rejects with a tier-related error, your account does not include Beta-tier commands. Visit https://mementoagi.com/dashboard/plan to upgrade.
