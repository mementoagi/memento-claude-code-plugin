---
description: End session — consolidate memories, create handoff, reset state.
---

Call the `get-command-instructions` MCP tool with `command: "sleep"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/sleep.md` and follow those instructions.
