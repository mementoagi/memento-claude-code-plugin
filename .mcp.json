{
  "mcpServers": {
    "memento-cloud": {
      "command": "npx",
      "args": ["-y", "memento-mcp-client@latest"],
      "env": {
        "MEMENTO_API_KEY": "${user_config.memento_api_key}"
      }
    }
  }
}
