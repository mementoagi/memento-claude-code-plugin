---
description: Show all your open PRs with their status and review state.
---

Call the `get-command-instructions` MCP tool with `command: "beta/show-open-prs.md"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/beta/show-open-prs.md` and follow those instructions.

If the cloud rejects with a tier-related error, your account does not include Beta-tier commands. Visit https://mementoagi.com/dashboard/plan to upgrade.
