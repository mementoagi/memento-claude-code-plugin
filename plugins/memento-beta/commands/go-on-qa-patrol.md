---
description: Patrol open PRs/branches for QA gaps and missing test coverage.
---

Call the `get-command-instructions` MCP tool with `command: "beta/go-on-qa-patrol.md"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/beta/go-on-qa-patrol.md` and follow those instructions.

If the cloud rejects with a tier-related error, your account does not include Beta-tier commands. Visit https://mementoagi.com/dashboard/plan to upgrade.
