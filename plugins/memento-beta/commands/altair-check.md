---
description: Run the Altair lint/check suite on the current code.
---

Call the `get-command-instructions` MCP tool with `command: "altair-check.md"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/altair-check.md` and follow those instructions.

If the cloud rejects with a tier-related error, your account does not include Beta-tier commands. Visit https://mementoagi.com/dashboard/plan to upgrade.
