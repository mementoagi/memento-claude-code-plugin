---
description: Patrol the codebase looking for architectural smells and risks.
---

Call the `get-command-instructions` MCP tool with `command: "beta/go-on-architecture-patrol.md"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/beta/go-on-architecture-patrol.md` and follow those instructions.

If the cloud rejects with a tier-related error, your account does not include Beta-tier commands. Visit https://mementoagi.com/dashboard/plan to upgrade.
