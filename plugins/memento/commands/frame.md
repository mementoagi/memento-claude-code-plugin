---
description: Checkpoint — assess current state, surface reminders, propose next action.
---

Call the `get-command-instructions` MCP tool with `command: "frame"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/frame.md` and follow those instructions.
