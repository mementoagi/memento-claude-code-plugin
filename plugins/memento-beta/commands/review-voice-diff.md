---
description: Review a diff between two voice profiles and reconcile.
---

Call the `get-command-instructions` MCP tool with `command: "beta/review-voice-diff.md"` and execute the returned instructions.

If `get-command-instructions` is unavailable, call `read-node` with path `core/commands/beta/review-voice-diff.md` and follow those instructions.

If the cloud rejects with a tier-related error, your account does not include Beta-tier commands. Visit https://mementoagi.com/dashboard/plan to upgrade.
